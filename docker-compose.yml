services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: shortlist
      POSTGRES_PASSWORD: shortlist
      POSTGRES_DB: shortlist
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - shortlist_data:/var/lib/postgresql/data
volumes:
  shortlist_data:
